#include "main.h"

/**
 * mul - Multiplies two numbers
 * @t: The first number to multiply
 * @x: The second number to multiply with first number
 * Return: The result of multiplication between x param and y param
 */

int mul(int t, int x)
{
	return (t * x);

}
